<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TaskManager</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="/css/homepage.css">

</head>

<body class="bg-gradient-to-br from-purple-700 to-blue-600 min-h-screen text-white font-sans flex flex-col items-center">
  <header class="w-full py-6 flex justify-between items-center px-8">
    <a class="text-4xl font-extrabold tracking-wider text-glow" href="#">TaskManager</a>
    <nav class="space-x-8 text-lg sticky">
      <a href="#features" class="hover:text-yellow-300">Features</a>
      <a href="#about" class="hover:text-yellow-300">About</a>
      <a href="#contact" class="hover:text-yellow-300">Contact</a>
      <a href="/dashboard" class="hover:text-yellow-300">Dashboard</a>
    </nav>
  </header>

  <main class="flex flex-col items-center text-center my-12 px-8 animate-fade-in">
    <section>
      <h1 class="text-6xl md:text-7xl font-extrabold leading-tight mb-6 text-glow">Level Up with <span class="text-yellow-300">TaskManager</span></h1>
      <p class="text-xl md:text-2xl font-light max-w-2xl mb-8">Organize, track, and accomplish more with TaskManager — the free tool designed to simplify your workday.</p>
      <div class="flex space-x-4 mb-12">
        <a href="/register" class="bg-yellow-300 text-purple-800 text-lg font-semibold px-8 py-3 rounded-full button-bounce">Get Started</a>
        <a href="#features" class="border border-yellow-300 text-lg font-semibold px-8 py-3 rounded-full button-bounce">Explore</a>
      </div>
    </section>

    <section id="features" class="w-full py-16 bg-gray-900 bg-opacity-50 px-8 glass rounded-t-3xl mb-12">
      <h2 class="text-center text-4xl font-semibold text-yellow-300 mb-10">All-in-One Features</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-10 max-w-6xl mx-auto">
        <div class="bg-gradient-to-br from-white to-gray-100 p-6 rounded-lg shadow-lg text-gray-800 feature-card glass">
          <h3 class="text-2xl font-semibold mb-3 text-blue-600">Effortless Task Management</h3>
          <p class="text-lg">Organize your tasks, set priorities, and manage deadlines with ease to stay on track.</p>
        </div>
        <div class="bg-gradient-to-br from-white to-gray-100 p-6 rounded-lg shadow-lg text-gray-800 feature-card glass">
          <h3 class="text-2xl font-semibold mb-3 text-blue-600">Accurate Time Tracking</h3>
          <p class="text-lg">Track the time spent on tasks to understand your work patterns and optimize productivity.</p>
        </div>
        <div class="bg-gradient-to-br from-white to-gray-100 p-6 rounded-lg shadow-lg text-gray-800 feature-card glass">
          <h3 class="text-2xl font-semibold mb-3 text-blue-600">Real-time Dashboard</h3>
          <p class="text-lg">Get a comprehensive view of your tasks, projects, and time to make data-driven decisions.</p>
        </div>
      </div>
    </section>

    <section id="about" class="py-16 w-full px-8 bg-gradient-to-br from-purple-600 to-blue-600 text-center mb-12">
      <h2 class="text-4xl font-semibold text-white mb-6">Why TaskManager?</h2>
      <p class="max-w-3xl mx-auto text-lg mb-8 text-gray-200">TaskManager empowers everyone to keep track of their tasks, manage time, and stay productive with no cost. Start optimizing your workflow today!</p>
      <div class="flex flex-col md:flex-row justify-center gap-8 max-w-4xl mx-auto">
        <div class="bg-purple-500 p-6 rounded-lg text-center glass">
          <h3 class="text-2xl font-semibold text-yellow-300 mb-2">Always Free</h3>
          <p>TaskManager is free for everyone, with no hidden fees or surprises. Focus on achieving more!</p>
        </div>
        <div class="bg-blue-500 p-6 rounded-lg text-center glass">
          <h3 class="text-2xl font-semibold text-yellow-300 mb-2">Secure & Reliable</h3>
          <p>Your data is safe with us. Enjoy a secure place to store and manage your tasks effectively.</p>
        </div>
      </div>
    </section>

    <section id="contact" class="py-16 w-full px-8 bg-gray-900 bg-opacity-70 glass rounded-b-3xl text-center mb-12">
      <h2 class="text-4xl font-semibold text-yellow-300 mb-8">Get in Touch</h2>
      <form class="max-w-md mx-auto flex flex-col items-center">
        <input type="email" placeholder="Enter your email" class="w-full mb-4 px-4 py-3 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-yellow-300">
        <button type="button" class="bg-yellow-300 text-purple-800 text-lg font-semibold px-8 py-3 rounded-lg button-bounce">Sign Up</button>
      </form>
      <p class="text-sm mt-4 text-gray-300">By signing up, you agree to our <button onclick="showterms()" class="underline">Terms & Conditions </button></a>.</p>
    </section>
  </main>

  <footer class="py-4 bg-gray-800 text-center text-white mt-12 w-full">
    <p>&copy; 2024 TaskManager. All rights reserved.</p>
    <div class="space-x-4">
      <button onclick="showterms()" class="underline">Terms of Service</button>
    </div>
  </footer>

  <div id="funFactSidebar" class="fun-fact-sidebar">
    <h2 class="text-2xl font-semibold text-purple-800 mb-2">Did you know?</h2>
    <p id="funFactText" class="text-lg text-gray-800"></p>
    <button class="close-btn mt-4" onclick="closePopup()">Close</button>
  </div>

  <dialog id="terms&conditions" class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-lg" close>
    <h2 class="text-2xl font-semibold mb-4">Terms & Conditions</h2>
    <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 font-bold">Please read these terms and conditions carefully before using our service.</p>
    <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 font-bold">By using our service, you agree to the following terms:</p>
    <ul class="list-disc list-inside">
      <li class="text-sm text-gray-600 dark:text-gray-400 mb-2 font-bold">You must be at least 13 years old to use our service.</li>
      <li class="text-sm text-gray-600 dark:text-gray-400 mb-2 font-bold">We reserve the right to change or update these terms at any time.</li>
      <li class="text-sm text-gray-600 dark:text-gray-400 mb-2 font-bold">We are not responsible for any consequences of using our service.</li>

    </ul>
    <button class="dialog_close-btn mt-4" onclick="closeDialog()">Close</button>
  </dialog>

  <script>
    async function showterms() {
      const dialog_box = document.getElementById('terms&conditions');
      dialog_box.showModal();
    }

    function closeDialog() {
      const dialog_box = document.getElementById('terms&conditions');
      dialog_box.close();
    }

    const funFactSidebar = document.getElementById('funFactSidebar');
    async function fetchFunFact() {
      try {
        const response = await fetch('https://api.api-ninjas.com/v1/facts', {
          headers: {
            'X-Api-Key': 'Your api key : D ',
          }
        });
        const data = await response.json();
        document.getElementById("funFactText").innerText = data[0].fact;
        funFactSidebar.classList.add('show');
        setTimeout(() => {
          funFactSidebar.classList.remove('show');
        }, 5000);
      } catch (error) {
        console.error("Error fetching fun fact:", error);
      }
    }

    function closePopup() {
      funFactSidebar.classList.remove('show');
    }

    window.onload = function () {
      fetchFunFact();
    };
  </script>

</body>
</html>
